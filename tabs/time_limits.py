import streamlit as st
import pandas as pd

def render():
    st.header("⏳ กำหนดเวลาตามกฎหมาย (Time Limits)")
    st.caption("อ้างอิง: พระราชบัญญัติศุลกากร พ.ศ. 2560 (ฉบับปรับปรุง)")

    # ==============================
    # 🚢 ส่วนที่ 1: ทางเรือ
    # ==============================
    st.subheader("🚢 1. ทางเรือ (Sea)")
    
    # Timeline ทั่วไป
    c1, c2, c3 = st.columns(3)
    with c1:
        st.error("24 ชม.")
        st.caption("เรือถึง -> รายงาน")
        st.markdown("*ยื่น Manifest + ทะเบียนเรือ*")
    with c2:
        st.info("10 วัน")
        st.caption("เรือถึง -> ย้ายของ")
        st.markdown("*เคลียร์พื้นที่ (Move Goods)*")
    with c3:
        st.warning("14 วัน")
        st.caption("เริ่มบรรทุก -> เก็บค่าเฝ้า")
        st.markdown("*ค่าธรรมเนียมเฝ้าเรือ*")

    c4, c5, c6 = st.columns(3)
    with c4:
        st.error("21 วัน")
        st.caption("เรือถึง -> กักเรือ")
        st.markdown("*ขนไม่หมด สั่งกักเรือ*")
    with c5:
        st.success("3 เดือน")
        st.caption("ค้าชายฝั่ง -> ตรวจบัญชี")
        st.markdown("*เก็บรักษาบัญชีสินค้า*")
    with c6:
        st.info("30 วัน")
        st.caption("ในอารักขา -> ตกค้าง")
        st.markdown("*เริ่มเป็นของตกค้าง*")

    st.markdown("---")
    st.markdown("#### 🚨 กรณีส่งของออกไม่ทัน (Short Shipment) - ทางเรือ")
    
    col_sea_ss1, col_sea_ss2, col_sea_ss3 = st.columns(3)
    with col_sea_ss1:
        st.warning("1. แจ้งเหตุ")
        st.markdown("ภายใน **3 วัน**")
        st.caption("นับแต่เรือออก")
    with col_sea_ss2:
        st.info("2. ส่งออกใหม่")
        st.markdown("ภายใน **21 วัน**")
        st.caption("นับแต่เรือออก")
    with col_sea_ss3:
        st.success("3. ขอรับคืน")
        st.markdown("ภายใน **30 วัน**")
        st.caption("นับแต่แจ้งเหตุ")

    st.divider()

    # ==============================
    # ✈️ ส่วนที่ 2: ทางอากาศ
    # ==============================
    st.subheader("✈️ 2. ทางอากาศ (Air)")
    
    st.markdown("**นิยามความสำเร็จ:** 🛬 เข้า = ถึงสนามบิน / 🛫 ออก = ออกจากสนามบินสุดท้าย")
    
    st.markdown("#### 🚨 กรณีส่งของออกไม่ทัน (Short Shipment) - ทางอากาศ")
    
    col_air_ss1, col_air_ss2, col_air_ss3 = st.columns(3)
    with col_air_ss1:
        st.warning("1. แจ้งเหตุ")
        st.markdown("ภายใน **3 วัน**")
        st.caption("นับแต่เครื่องออก")
    with col_air_ss2:
        st.info("2. ส่งออกใหม่")
        st.markdown("ภายใน **7 วัน**")
        st.caption("นับแต่เครื่องออก")
    with col_air_ss3:
        st.success("3. ขอรับคืน")
        st.markdown("ภายใน **30 วัน**")
        st.caption("นับแต่แจ้งเหตุ")

    st.divider()

    # ==============================
    # 🚛 ส่วนที่ 3: ทางบก
    # ==============================
    st.subheader("🚛 3. ทางบก (Land)")
    
    col_land1, col_land2 = st.columns(2)
    with col_land1:
        st.markdown("**การผ่านแดน:**")
        st.info("เข้าด่าน 'โดยพลัน' (ทันที)")
    with col_land2:
        st.markdown("**เงินติดตัวออก:**")
        st.success("ไม่เกิน 2 ล้านบาท")

    st.markdown("#### 🚨 กรณีส่งของออกไม่ทัน (Short Shipment) - ทางบก")
    
    col_land_ss1, col_land_ss2, col_land_ss3 = st.columns(3)
    with col_land_ss1:
        st.warning("1. แจ้งเหตุ")
        st.markdown("ภายใน **10 วัน**")
        st.caption("นับแต่วันตรวจปล่อย")
    with col_land_ss2:
        st.info("2. ส่งออกใหม่")
        st.markdown("ภายใน **14 วัน**")
        st.caption("นับแต่วันตรวจปล่อย")
    with col_land_ss3:
        st.success("3. ขอรับคืน")
        st.markdown("ภายใน **30 วัน**")
        st.caption("นับแต่แจ้งเหตุ")

    st.divider()

    # ==============================
    # 📮 ส่วนที่ 4: ทางไปรษณีย์
    # ==============================
    st.subheader("📮 4. ทางไปรษณีย์ (Postal)")
    st.info("📥 นำเข้า = เปิดถุง / 📤 ส่งออก = ปิดถุง")
    
    with st.container(border=True):
        st.markdown("##### ⚠️ ระยะเวลาสำหรับไปรษณีย์")
        st.markdown("""
        กฎหมายไม่ได้กำหนดเวลาแยกเฉพาะสำหรับทางไปรษณีย์ แต่ให้ **ยึดตามช่องทางการขนส่งที่ใช้จริง**
        * หากขนส่งทาง **เรือ** -> ใช้เกณฑ์ทางเรือ
        * หากขนส่งทาง **อากาศ** -> ใช้เกณฑ์ทางอากาศ
        * หากขนส่งทาง **บก** -> ใช้เกณฑ์ทางบก
        """)

    st.divider()

    # ==============================
    # ⚡ EXAM CHEAT SHEET (ตารางสรุปใหม่)
    # ==============================
    st.header("⚡ สรุปสูตรจำสอบ: กรณีส่งออกไม่ทัน (Short Shipment)")
    st.caption("เปรียบเทียบระยะเวลาแจ้งเหตุ / ส่งออกใหม่ / รับคืน ตามโหมดขนส่ง")

    short_ship_data = [
        {
            "โหมด": "🚢 ทางเรือ", 
            "1. แจ้งเหตุ": "3 วัน (นับเรือออก)", 
            "2. ส่งออกใหม่": "21 วัน (นับเรือออก)", 
            "3. รับคืน": "30 วัน (นับแจ้งเหตุ)"
        },
        {
            "โหมด": "✈️ ทางอากาศ", 
            "1. แจ้งเหตุ": "3 วัน (นับเครื่องออก)", 
            "2. ส่งออกใหม่": "7 วัน (นับเครื่องออก)", 
            "3. รับคืน": "30 วัน (นับแจ้งเหตุ)"
        },
        {
            "โหมด": "🚛 ทางบก", 
            "1. แจ้งเหตุ": "10 วัน (นับตรวจปล่อย)", 
            "2. ส่งออกใหม่": "14 วัน (นับตรวจปล่อย)", 
            "3. รับคืน": "30 วัน (นับแจ้งเหตุ)"
        }
    ]
    df_short_ship = pd.DataFrame(short_ship_data)
    st.dataframe(
        df_short_ship, 
        use_container_width=True, 
        hide_index=True,
        column_config={
            "โหมด": st.column_config.TextColumn("โหมด", width="small"),
            "1. แจ้งเหตุ": st.column_config.TextColumn("1. แจ้งเหตุ (Notify)", width="medium"),
            "2. ส่งออกใหม่": st.column_config.TextColumn("2. ส่งออกใหม่ (Re-export)", width="medium"),
            "3. รับคืน": st.column_config.TextColumn("3. รับคืน (Return)", width="medium"),
        }
    )
    
    st.info("💡 **ข้อสังเกต:** การขอรับคืนมีระยะเวลาเท่ากันคือ **30 วัน** (นับจากแจ้งเหตุ) ทุกโหมด")
